import 'package:flutter/material.dart';

class StatelessSabak extends StatelessWidget {
  const StatelessSabak({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
